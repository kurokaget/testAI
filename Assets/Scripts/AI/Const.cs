public class Const {
	public enum AIController{
		Hide,
		TemporaryEvacuation,
		Assault,
		Advance,
		Wait,
	}

	public enum AssaultAIController {
	}

	public enum TeamType {
		A, B
	}
}